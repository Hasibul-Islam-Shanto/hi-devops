---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const allPosts = await getCollection('blog', ({ data }) => !data.draft);
const posts = allPosts.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

function formatDate(date: Date) {
  return date.toLocaleDateString('en-GB', { day: 'numeric', month: 'long', year: 'numeric' });
}
---

<BaseLayout title="Blog" description="Thoughts and write-ups from my DevOps learning journey.">
  <section class="blog-hero">
    <p class="section-eyebrow">Blog</p>
    <h1 class="blog-title">
      Writing that <span>shares what I learn.</span>
    </h1>
    <p class="blog-desc">
      Notes on DevOps fundamentals, deployment workflows, architecture decisions, and tools I use
      while learning by building.
    </p>
  </section>

  <section class="blog-content">
    {
      posts.length === 0 ? (
        <div class="empty-state">
          <div class="empty-icon">
            <svg
              width="32"
              height="32"
              viewBox="0 0 24 24"
              fill="none"
              stroke="var(--color-text-muted)"
              stroke-width="1.5"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" />
              <polyline points="14 2 14 8 20 8" />
              <line x1="16" y1="13" x2="8" y2="13" />
              <line x1="16" y1="17" x2="8" y2="17" />
              <polyline points="10 9 9 9 8 9" />
            </svg>
          </div>
          <h2 class="empty-title">No posts yet</h2>
          <p class="empty-desc">First post coming soon. Check back later!</p>
        </div>
      ) : (
        <div class="posts-grid">
          {posts.map((post) => (
            <a href={`/blog/${post.id}`} class="card-premium post-card">
              <div class="post-meta">
                <time datetime={post.data.pubDate.toISOString()} class="post-date">
                  {formatDate(post.data.pubDate)}
                </time>
                <span class="post-read-time">{post.data.readTime}</span>
              </div>
              <h2 class="post-title">{post.data.title}</h2>
              <p class="post-desc">{post.data.description}</p>
              {post.data.tags.length > 0 && (
                <div class="post-tags">
                  {post.data.tags.map((tag: string) => (
                    <span class="post-tag">{tag}</span>
                  ))}
                </div>
              )}
            </a>
          ))}
        </div>
      )
    }
  </section>
</BaseLayout>

<style>
  .blog-hero {
    max-width: 1000px;
    margin: 0 auto;
    padding: 2rem 1rem;
  }

  .section-eyebrow {
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: var(--color-aurora-4);
    margin-bottom: 1.35rem;
  }

  .blog-title {
    max-width: 760px;
    font-size: clamp(2.35rem, 2vw, 4rem);
    font-weight: 800;
    color: var(--color-text-primary);
    letter-spacing: -0.02em;
    line-height: 1.12;
    margin-bottom: 1rem;
  }

  .blog-title span {
    color: var(--color-aurora-1);
  }

  .blog-desc {
    max-width: 800px;
    font-size: clamp(1rem, 2vw, 1rem);
    color: var(--color-text-secondary);
    line-height: 1.65;
  }

  .blog-content {
    max-width: 1000px;
    margin: 0 auto;
    padding: 0 1.5rem 5rem;
  }

  .empty-state {
    max-width: 360px;
    margin: 4rem auto;
    text-align: center;
    padding: 3rem 2rem;
    border-radius: 16px;
    background: var(--color-panel-bg);
    border: 1px solid var(--color-panel-border);
    box-shadow: var(--card-shadow);
  }

  .empty-icon {
    width: 64px;
    height: 64px;
    border-radius: 16px;
    background: var(--color-table-header);
    border: 1px solid var(--color-panel-border);
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 1.25rem;
  }

  .empty-title {
    font-size: 1.125rem;
    font-weight: 700;
    color: var(--color-text-primary);
    margin-bottom: 0.5rem;
  }

  .empty-desc {
    color: var(--color-text-muted);
    font-size: 0.875rem;
  }

  .posts-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
    gap: 1.25rem;
  }

  .post-card {
    display: block;
    padding: 1.5rem;
    text-decoration: none;
  }

  .post-meta {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-bottom: 0.75rem;
  }

  .post-date {
    font-size: 0.8125rem;
    color: var(--color-text-muted);
  }

  .post-read-time {
    font-size: 0.75rem;
    color: var(--color-text-muted);
    padding: 0.15rem 0.5rem;
    border-radius: 4px;
    background: var(--color-table-header);
    border: 1px solid var(--color-panel-border);
  }

  .post-title {
    font-size: 1.125rem;
    font-weight: 700;
    color: var(--color-text-primary);
    margin-bottom: 0.5rem;
    line-height: 1.35;
  }

  .post-desc {
    font-size: 0.875rem;
    color: var(--color-text-secondary);
    line-height: 1.6;
    margin-bottom: 1rem;
  }

  .post-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.4rem;
  }

  .post-tag {
    font-size: 0.7rem;
    font-weight: 600;
    padding: 0.2em 0.6em;
    border-radius: 4px;
    background: rgba(99, 102, 241, 0.12);
    color: var(--color-text-accent);
    border: 1px solid rgba(99, 102, 241, 0.2);
    text-transform: lowercase;
  }
</style>
